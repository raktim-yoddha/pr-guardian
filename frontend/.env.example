# Base URL of the PR Guardian backend API.
# Local dev default is http://localhost:8000. In Docker/prod point this at the
# public backend URL (or the Nginx origin).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
